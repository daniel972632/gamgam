# Gamgam

Lightweight travel planning MVP built with Next.js, Prisma, PostgreSQL/Supabase, and Vercel in mind.

## Run locally

```bash
npm install
npm run dev
```

Open `http://localhost:3000`.

## Environment

Copy `.env.example` to `.env` and fill in Supabase/PostgreSQL values when backend integration starts.
